# fend-capstone
Capstone project of Udacity's Front End Nanodegree program. A travel booking site clone that pulls data from a local server as well as multiple external APIs.

## Installation
run ``` npm install ``` in parent directory to install Node.js' dependancies.