import { tripFormHandler } from '../src/client/js/app.js';

describe('tripFormHandler() test', () => {
  test('tripFormHandler() should exist', () => {
    expect(tripFormHandler).toBeDefined();
  });
});
